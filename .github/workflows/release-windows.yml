name: Windows release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Test server and build agent distribution
        shell: pwsh
        run: |
          Push-Location mobile
          npm test
          Pop-Location
          .\gradlew.bat :server:test :agent:test :agent:installDist --no-daemon

      - name: Resolve release version
        id: version
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}" -replace '^v', ''
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            $properties = Get-Content gradle.properties -Raw
            $match = [regex]::Match($properties, '(?m)^netscanVersion=(\d+\.\d+\.\d+)\r?$')
            if (-not $match.Success) { throw 'Не удалось определить версию агента' }
            $version = $match.Groups[1].Value
          }
          $declared = ((Get-Content gradle.properties | Where-Object { $_ -like 'netscanVersion=*' }) -split '=', 2)[1]
          if ($version -ne $declared) { throw "Версия тега $version не совпадает с netscanVersion=$declared" }
          "version=$version" >> $env:GITHUB_OUTPUT

      - name: Build self-contained Windows application
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force build\package | Out-Null
          $mainJar = (Get-ChildItem agent\build\install\netscan\lib\agent-*.jar | Select-Object -First 1).Name
          jpackage `
            --type app-image `
            --name netscan `
            --app-version "${{ steps.version.outputs.version }}" `
            --input agent\build\install\netscan\lib `
            --main-jar $mainJar `
            --main-class ru.ruznak.netscan.MainKt `
            --java-options "-Dfile.encoding=UTF-8" `
            --java-options "-Dstdout.encoding=UTF-8" `
            --java-options "-Dstderr.encoding=UTF-8" `
            --dest build\package
          New-Item -ItemType Directory -Force build\artifact | Out-Null
          Copy-Item build\package\netscan build\artifact\netscan -Recurse
          Copy-Item packaging\windows\install.cmd build\artifact\install.cmd
          Copy-Item packaging\windows\install-netscan.ps1 build\artifact\install-netscan.ps1
          Copy-Item packaging\windows\uninstall-netscan.ps1 build\artifact\uninstall-netscan.ps1
          Copy-Item packaging\windows\INSTALL.txt build\artifact\INSTALL.txt
          Copy-Item packaging\windows\update-netscan.ps1 build\artifact\netscan\update-netscan.ps1
          # Windows PowerShell 5.1 treats UTF-8 without BOM as an ANSI code page.
          # Re-encode scripts so Russian text cannot break PowerShell parsing.
          $installScript = Get-Content build\artifact\install-netscan.ps1 -Raw
          $installScript | Set-Content build\artifact\install-netscan.ps1 -Encoding utf8BOM -NoNewline
          $uninstallScript = Get-Content build\artifact\uninstall-netscan.ps1 -Raw
          $uninstallScript | Set-Content build\artifact\uninstall-netscan.ps1 -Encoding utf8BOM -NoNewline
          $updateScript = Get-Content build\artifact\netscan\update-netscan.ps1 -Raw
          $updateScript | Set-Content build\artifact\netscan\update-netscan.ps1 -Encoding utf8BOM -NoNewline
          Compress-Archive `
            -Path build\artifact\* `
            -DestinationPath "build\netscan-windows-${{ steps.version.outputs.version }}.zip"
          $zip = "build\netscan-windows-${{ steps.version.outputs.version }}.zip"
          $hash = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  netscan-windows-${{ steps.version.outputs.version }}.zip" |
            Set-Content "build\netscan-windows-${{ steps.version.outputs.version }}.sha256"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: netscan-windows-${{ steps.version.outputs.version }}
          path: |
            build/netscan-windows-*.zip
            build/netscan-windows-*.sha256

      - name: Publish GitHub release asset
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/netscan-windows-*.zip
            build/netscan-windows-*.sha256
          generate_release_notes: true
